const axios = require("axios");

export async function getBookings(userId) {
  //TO DO once endpoint ready
  //  const response = await axios.get(`/flight-search/inspire?origin=${origin}`);
  //  return response.data;
}
